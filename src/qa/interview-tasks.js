const wait = () => {};

// wait(2000).then(() => {
//   console.log('waited');
// });

// Promise.all([Promise.resolve(21), Promise.resolve(4)]).then((res) =>
//   console.log(res, '--PromiseAll')
// );
// [21, 4]

const promiseAll = () => {};

// promiseAll([Promise.resolve(21), Promise.reject(4)]).then((res) =>
//   console.log(res)
// );

// Promise.allSettled([Promise.resolve(21), Promise.reject(4)]).then((res) =>
//   console.log(res, '--allSettled')
// );

const allSettled = () => {};

const useOnMount = () => {};
const useWatch = () => {};
